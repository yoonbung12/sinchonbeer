<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="MypageMapper">
	
	<!-- 회원 정보 조회 -->
	<select id="readMember" resultType="com.bitcamp.sc.member.domain.Member">
		select * from member where midx=#{idx}
	</select>

	<!-- 회원 정보 수정 -->
	<update id="updateMember">
		update member set mname=#{name}, mpw=#{newpw}, mphone=#{phone} where memail=#{email}
	</update>

	<!-- 회원 탈퇴 -->
	<delete id="deleteMember">
		delete from member where memail=#{email} and mpw=#{pw}
	</delete>
</mapper>